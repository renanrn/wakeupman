---
clickup:
  task_id: ""
  epic_task_id: ""
  list: "Backlog"
  url: ""
  last_sync: ""
executor: "@ux-design-expert"
quality_gate: "@dev"
quality_gate_tools: [ui_validation]
---

# Story 2.3: Calibration UI for user baseline

## Status
Done

## Story
**As a** User,
**I want** to calibrate my normal awake face,
**so that** the app accurately detects my specific drowsiness signs.

## Acceptance Criteria
1. UI screen to calculate user's baseline eye openness.
2. Save baseline to DataStore.

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

## Tasks / Subtasks
- [x] Task 1: Create `CalibrationScreen` with a camera preview.
- [x] Task 2: Guide user to keep eyes open for 3 seconds.
- [x] Task 3: Average the `eyeOpenProbability` over the period.
- [x] Task 4: Save calculated baseline to Jetpack `DataStore`.

## Dev Notes
Normal eye openness varies, especially with glasses. If baseline is 0.7, the threshold for closed might be 0.4.

### Testing
- [x] Verify calibration completes in 3 seconds.
- [x] Verify baseline value is persisted across app restarts.

## Change Log
| Date | Version | Description | Author |
|---|---|---|---|
| 2026-04-27 | 1.0 | Initial draft | River |
| 2026-04-29 | 2.3 | Calibration UI implemented | Dex |

## Dev Agent Record

### Agent Model Used
Gemini 2.0 Flash

### Debug Log References
- Added `datastore-preferences`, `camera-view` and `hilt-navigation-compose` to `build.gradle.kts`.
- Created `PreferencesRepository` mapping `eye_open_baseline` key to a Flow and suspended save.
- Created `DataStoreModule` for Hilt DI.
- Developed `CalibrationViewModel` using `StateFlow` to manage Idle, Calibrating and Completed states over a 3-second window.
- Built `CalibrationScreen` with `AndroidView` for `PreviewView`, tying to ML Kit for eye open average.

### Completion Notes List
- Calibration calculates baseline successfully and persists to Jetpack DataStore correctly.

### File List
- app/build.gradle.kts
- app/src/main/java/com/wakeupman/data/PreferencesRepository.kt
- app/src/main/java/com/wakeupman/core/di/DataStoreModule.kt
- app/src/main/java/com/wakeupman/ui/calibration/CalibrationViewModel.kt
- app/src/main/java/com/wakeupman/ui/calibration/CalibrationScreen.kt

## QA Results
- [x] `ui_validation` by @architect: PASSED.
- [x] `CalibrationViewModel` correctly isolates StateFlow logic from UI.
- [x] Unidirectional data flow achieved (UI calls `startCalibration()`, reacts to state changes).
- [x] `DataStore` correctly injected with Hilt.
- [x] `CameraPreviewWithAnalysis` binds lifecycle safely and correctly shuts down `imageProxy` inside the ML Kit analyzer block.

Status: Technical Approval Granted. Ready for Merge.
