---
clickup:
  task_id: ""
  epic_task_id: ""
  list: "Backlog"
  url: ""
  last_sync: ""
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [unit_testing]
---

# Story 2.4: Detection Logic Engine (Threshold monitor)

## Status
Done

## Story
**As a** System,
**I want** to trigger alerts based on rules,
**so that** the user is warned instantly.

## Acceptance Criteria
1. `DrowsinessEngine` collects data from AI.
2. Triggers 'Warning' or 'Emergency' states based on rules.
3. Rule: Eyes closed (prob < 40% of baseline) for > 2s = Emergency.
4. Rule: Head nod detected = Emergency.

## 🤖 CodeRabbit Integration
> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.
> To enable, set `coderabbit_integration.enabled: true` in core-config.yaml

## Tasks / Subtasks
- [x] Task 1: Implement `StateFlow` for `VigilanceState` (Standby, Active, Warning, Emergency).
- [x] Task 2: Implement sliding window buffer for AI results.
- [x] Task 3: Evaluate rules on every new frame against the buffer.
- [x] Task 4: Emit State changes to be consumed by the Alert Manager.

## Dev Notes
Needs to be highly efficient. Use Coroutines Flow.

### Testing
- [x] Comprehensive unit tests for state machine transitions based on mock frame data.

## Change Log
| Date | Version | Description | Author |
|---|---|---|---|
| 2026-04-27 | 1.0 | Initial draft | River |
| 2026-04-29 | 2.4 | Threshold monitor logic engine implementation | Dex |

## Dev Agent Record

### Agent Model Used
Gemini 2.0 Flash

### Debug Log References
- Added `VigilanceState` enum and `StateFlow` to `DrowsinessEngine`.
- Modified `processFaceData` to accept eye openness probabilities.
- Re-implemented `pitch` processing and integrated a >2000ms rolling window for eye frames.
- Recreated `DrowsinessEngineTest` with Coroutine Test dispatcher and MockK for `PreferencesRepository`.

### Completion Notes List
- The logic engine accurately calculates thresholds based on injected DataStore baseline.
- `StateFlow` successfully updates to `WARNING` during closing transition and `EMERGENCY` upon meeting rules.
- Test coverage ensures <2s triggers correctly and validates state recovery.

### File List
- app/build.gradle.kts
- app/src/main/java/com/wakeupman/domain/DrowsinessEngine.kt
- app/src/main/java/com/wakeupman/data/MLKitFaceAnalyzer.kt
- app/src/test/java/com/wakeupman/domain/DrowsinessEngineTest.kt

## QA Results
- [x] `unit_testing` review by @architect: PASSED.
- [x] Tested mathematically correct threshold calculation (`baseline * 0.4`).
- [x] Test coverage properly utilizes `StandardTestDispatcher` via MockK, validating delays correctly.
- [x] Code architecture maintains strong `domain` purity, consuming configuration strictly via Dependency Injection.
- [x] Sliding window cleanup ensures memory scaling is constrained to ~O(1) constants.

Status: Technical Approval Granted. Ready for Merge.
